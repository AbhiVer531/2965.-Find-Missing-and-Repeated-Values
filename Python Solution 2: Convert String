class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        count = 0
        for num in nums:
            strInt = str(num)
            if len(strInt) % 2 == 0:
                count += 1
        return count 
